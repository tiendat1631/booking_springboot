Yêu cầu:

- [Node.js](https://nodejs.org/en) > 18.0.

Hướng dẫn cài đặt:

- Truy cập directory frontend: `cd frontend`.
- Cài đặt các package: `npm install`.
- Chạy server dev: `npm run dev`.
- Để quay về directory gốc, thực hiện câu lệnh `cd ..` khi đang ở directory frontend.